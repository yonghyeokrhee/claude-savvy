# Human-in-the-Loop Workflow

## 개념

긴 Workflow를 실행하다 보면 Claude가 중간에 잘못된 방향으로 가는 경우가 있습니다. **Human-in-the-Loop**는 핵심 결정 지점에 사람의 확인을 넣어 이 문제를 방지하는 패턴입니다.

Claude Code는 `AskUserQuestion` 도구를 통해 작업 중 언제든 사용자에게 질문하고 답변을 기다릴 수 있습니다.

## AskUserQuestion 이란?

Claude Code가 내부적으로 사용하는 도구로, 작업을 **일시 중단**하고 사용자에게 질문을 던집니다. 사용자가 답변하면 그 내용을 바탕으로 작업을 이어갑니다.

```
Claude 작업 실행
    ↓
중요한 결정 지점 도달
    ↓
AskUserQuestion 호출 → 사용자에게 질문
    ↓
사용자 답변 수신
    ↓
답변 기반으로 작업 계속
```

프롬프트에서 직접 유도할 수 있습니다:

```
"각 단계 시작 전에 계획을 먼저 알려주고, 내가 확인하면 진행해줘."
"중간에 불확실한 부분이 있으면 진행하지 말고 먼저 물어봐."
```

---

## 실습 예제 — AskUserQuestion으로 "주말 여행 일정" 짜기

특별한 데이터나 사전 지식 없이 **누구나 따라 할 수 있는** 예제로 Human-in-the-Loop을 경험합니다. Claude가 혼자 다 정해버리는 대신, **결정이 필요한 순간마다 선택지를 제시해 물어보게** 만듭니다.

### 실습 목표

Claude가 임의로 가정하고 끝까지 달려가는 대신, **선택지 제시 → 사용자가 고름 → 그 답을 반영**하는 흐름을 직접 경험합니다.

### 프롬프트

```
이번 주말 1박 2일 여행 일정을 짜줘.

단, 시작 전에 결정이 필요한 부분은 네 마음대로 정하지 말고,
선택지를 제시해서 나에게 하나씩 물어봐줘.
내 답을 모두 받은 다음에 최종 일정표를 완성해줘.
```

### AskUserQuestion은 어떻게 보이나

`AskUserQuestion`은 단순히 텍스트로 묻는 게 아니라, **버튼처럼 고를 수 있는 선택지(2~4개)**를 제시합니다. 각 질문에는 짧은 라벨(헤더)이 붙고, 사용자는 언제든 **"기타"로 직접 입력**할 수도 있습니다.

```
┌────────────────────────────────────────┐
│ [여행 스타일]                            │  ← 헤더(짧은 라벨)
│ 어떤 분위기의 여행을 원하세요?           │  ← 질문
│  ○ 휴양      느긋하게 쉬기               │
│  ○ 관광      명소·볼거리 위주            │  ← 선택지 + 설명
│  ○ 액티비티  체험·레저 중심              │
│  ○ 미식      맛집 탐방                   │
│  ○ 기타      (직접 입력)                 │
└────────────────────────────────────────┘
```

### 실행 흐름

```
[질문 1] 여행 스타일
  Claude(AskUserQuestion): 휴양 / 관광 / 액티비티 / 미식
  사용자: [관광] 선택
        ↓
[질문 2] 1인 예산
  Claude(AskUserQuestion): 10만원 이하 / 10~30만원 / 30만원 이상
  사용자: [10~30만원] 선택
        ↓
[질문 3] 이동 수단
  Claude(AskUserQuestion): 자가용 / 대중교통 / 상관없음
  사용자: [대중교통] 선택
        ↓
[완성] 세 선택(관광·중예산·대중교통)을 반영한 1박 2일 일정표 작성
```

선택을 바꿔 다시 실행해 보면, **같은 요청이라도 답에 따라 결과가 달라지는** 것을 바로 확인할 수 있습니다.

> 💡 업무에서도 똑같습니다. "보고서를 써줘"(대상 독자·분량·톤을 옵션으로), "이 폴더를 정리해줘"(정리 기준을 옵션으로 받고, 파일 이동 전 한 번 더 확인)처럼 **사람이 정해야 할 결정**을 옵션으로 받아 진행하면 됩니다.

### 확인 포인트가 없을 때 vs 있을 때

| 구분 | 확인 포인트 없음 | Human-in-the-Loop |
|---|---|---|
| 속도 | 빠름 | 느림 |
| 방향 수정 | 끝나고 나서야 가능 | 중간에 즉시 수정 |
| 실수 비용 | 전체 재실행 | 해당 단계부터 재실행 |
| 적합한 상황 | 결과가 예측 가능한 단순 작업 | 중요하거나 결과가 불확실한 작업 |

---

## Workflow 설계 팁

### 확인 포인트를 넣어야 할 순간

- **방향 결정이 필요한 분기점** — "A방식과 B방식 중 어느 걸로 할까요?"
- **비가역적인 작업 직전** — 파일 덮어쓰기, 외부 API 호출, DB 수정
- **불확실한 데이터** — "이 수치가 맞는지 확인이 필요합니다"
- **긴 작업의 중간 결과 검토** — "여기까지 맞게 가고 있는지 확인해주세요"

### 확인 포인트 없어도 되는 순간

- 읽기 전용 탐색 (파일 구조 파악, 코드 분석)
- 명확하게 정의된 반복 작업
- 결과를 쉽게 되돌릴 수 있는 작업

### 프롬프트 패턴

```
# 패턴 1 — 계획 먼저 확인
"작업 시작 전에 전체 계획을 먼저 보여주고, 내가 OK하면 실행해줘."

# 패턴 2 — 단계별 게이트
"각 단계가 끝나면 다음 단계 진행 여부를 물어봐."

# 패턴 3 — 불확실 시 중단
"확실하지 않은 부분은 가정하지 말고 즉시 질문해줘."

# 패턴 4 — 결정 위임
"선택지가 있으면 내가 고를 수 있게 옵션을 제시해줘."
```
